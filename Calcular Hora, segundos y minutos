<?php

    //Responder en esta variable
    $segundos = 3601;

    $horas_calcular = (int)($segundos / 3600);

    $segundos = (int)($segundos % 60);

    $minutos =  (int)($segundos / 60);


    echo "El tiempo es $horas_calcular horas, $minutos minutos $segundos segundos,"

?>
